

export type QaWorkspace = { root: string, helloTxt: string, emptyTxt: string, bigTxt: string, dupA: string, dupB: string, dupC: string, uniqueInDup: string, compareA: string, compareB: string, checksumSha256: string, trashTarget: string, };
